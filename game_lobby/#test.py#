from test_emitted import tester1, tester2
from test_class import Tester
from login import LoginWindow
import sys, os, time
sys.path.append(os.path.join("../../"))
from waldo.lib import Waldo

HOSTNAME = '127.0.0.1'
PORT = 6922

def function1(variable):
    print 'blah'

Waldo.tcp_accept(tester2, HOSTNAME, PORT)
time.sleep(3)
tester_obj = Waldo.tcp_connect(tester1, HOSTNAME, PORT, LoginWindow)
time.sleep(3)
tester_obj.do_test()
tester_obj.set_message('hi there')
